package lectures.part2oop

object MethodNotation extends App {

  //All Operators are method.
  //unary_prefix only works with - + ~ !
  // A funtion which does not use any parameters can use as a postfix notation
  // A compiler see if an object being called like an method in scala it will look for the appply method in the object declartionn
  //postfix notation object methodname ie mary isAlive
  //prefix notation ie mary.unary_! cab be called !marry only applicable on - + ~ !
  //infix notation - only for method with one parameter
  //Apply method allow you to call the object as function
}
